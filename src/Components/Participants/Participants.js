import { React } from 'react';
import { AreaHeader } from '../../Shared/AreaHeader';

const Participants = () => {
  return <AreaHeader text='participants'></AreaHeader>;
};

export default Participants;
